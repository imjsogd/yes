name: Build Bums! Mod

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        config:
          - name: Android
            os: ubuntu-latest
            target: Android64
          - name: Windows
            os: windows-latest
            target: Win64

    name: Build (${{ matrix.config.name }})
    runs-on: ${{ matrix.config.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Build the mod
        uses: geode-sdk/build-geode-mod@v2
        with:
          target: ${{ matrix.config.target }}
          build-config: Release
          sdk: v5.7.1

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Bums-${{ matrix.config.name }}
          path: .build/*.geode
